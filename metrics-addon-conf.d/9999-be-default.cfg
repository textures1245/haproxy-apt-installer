# เปิดไฟล์นี้เพื่อกำหนดค่า backend เริ่มต้นสำหรับ HAProxy ในกรณีที่ไม่มีการจับคู่กับ backend อื่นๆ

# backend default_backend
#     balance roundrobin
#     option httpchk GET /
#     http-check expect status 200
#     server default1 127.0.0.1:8080 check  